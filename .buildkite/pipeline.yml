steps:
  - label: "🔄 Checkout"
    command: |
      echo "Código descargado correctamente"
      ls -la

  - label: "📦 Setup + Tests Backend"
    id: "backend"
    plugins:
      - docker#v5.11.0:
          image: "php:8.2-cli"
          always-pull: false
          command:
            - /bin/bash
            - -lc
            - |
              echo "Instalando dependencias PHP..."
              cp .env.example .env
              php -v
              curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
              composer install --no-interaction --no-progress
              echo "✅ Dependencias PHP instaladas"
              echo "Ejecutando tests PHPUnit..."
              php artisan test
              echo "✅ Tests backend completados"

  - label: "📦 Setup + Tests Frontend"
    id: "frontend"
    plugins:
      - docker#v5.11.0:
          image: "node:18"
          always-pull: false
          command:
            - /bin/bash
            - -lc
            - |
              echo "Instalando dependencias Node..."
              node -v
              npm -v
              npm install
              echo "✅ Dependencias Node instaladas"
              echo "Ejecutando tests Jest..."
              npm test
              echo "✅ Tests frontend completados"

  - label: "🏗️ Build"
    depends_on:
      - "backend"
      - "frontend"
    command: |
      echo "Creando artefactos..."
      mkdir -p build
      tar -czf build/app.tar.gz public app builds composer.json package.json .env 2>/dev/null
      echo "✅ Build completado (build/app.tar.gz)"

  - label: "🚀 Deploy Staging"
    command: |
      echo "Desplegando a staging (simulado)..."
      sleep 3
      echo "✅ Deploy exitoso"
    if: build.branch == "main"

timeout_in_minutes: 15
