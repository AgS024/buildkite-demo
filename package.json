{
  "name": "pokemon-marketplace",
  "version": "1.0.0",
  "scripts": {
    "build": "echo \"Building frontend...\" && mkdir -p ./builds && echo \"<h1>Buildkite Frontend OK</h1>\" | tee ./builds/index.html >/dev/null && echo \"✅ Build completed\"",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "react": "^18.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "roots": [
      "<rootDir>/tests"
    ]
  }
}
